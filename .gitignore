# Patterns that are specific to a text editor, IDE, operating system, or user
# environment are not added here. They should be added to your local gitignore
# file instead:
# https://docs.github.com/en/get-started/git-basics/ignoring-files#configuring-ignored-files-for-all-repositories-on-your-computer

# Build subdirectories.
/*build*
!/build-aux
!/build_msvc

*.pyc

# Only ignore unexpected patches
*.patch
!contrib/guix/patches/*.patch
!depends/patches/**/*.patch

/CMakeUserPresets.json

# Previous releases and staging
/releases
/release-staging
/release-bins
# /release is tracked for build/smoke scripts; binary output dirs are
# gitignored explicitly below (release/linux/, release/win64/) and the
# checksum file is gitignored as a build artifact.
release/SHA256SUMS.txt
release/build-release

# cargo default target dir
target/

/guix-build-*

/ci/scratch/

# Claude Code / AI assistant artifacts
.claude/
CLAUDE.md
IMPLEMENTATION_PLAN.md
ANN_THREAD.md

# Local infrastructure and deployment
/deploy/
/site/
/tools/embed_ptx.py
/tools/validate_chain.py

# Local scripts and tools
.python-version
check_merit.ps1
freycoin-testnet.bat
release/linux/
release/win64/
